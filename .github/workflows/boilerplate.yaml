name: Boilerplate

on:
  pull_request:
    branches: ['main']

jobs:

  check:
    name: Boilerplate Check
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false # Keep running if one leg fails.
      matrix:
        extension:
        - go
        - sh

        # Map between extension and human-readable name.
        include:
        - extension: go
          language: Go
        - extension: sh
          language: Bash

    steps:
      - name: Check out code
        uses: actions/checkout@v3

      - uses: chainguard-dev/actions/boilerplate@7a61f04a54bc92e46eac322269e0a05c807d3814 # main
        with:
          extension: ${{ matrix.extension }}
          language: ${{ matrix.language }}
